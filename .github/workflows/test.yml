name: test

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  go-test-linux:
    name: linux ${{ matrix.kernel }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - kernel: 4.4.302
            kernel-tag: v4.4.302
            force-9p: true
            version: v4.4-gcc-gnu89-hostcc-no-stack-9p
            compiler: gnu89
            kconfig: .github/kconfigs/legacy-kernel.config
            expect-pidfd: unavailable
          - kernel: 5.4.302
            kernel-tag: v5.4.302
            force-9p: true
            version: v5.4-9p
            compiler: default
            expect-pidfd: available
          - kernel: 5.15.211
            kernel-tag: v5.15.211
            force-9p: false
            version: v5.15-virtiofs
            compiler: default
            expect-pidfd: available
          - kernel: 6.6.144
            kernel-tag: v6.6.144
            force-9p: false
            version: v6.6-virtiofs
            compiler: default
            expect-pidfd: available
          - kernel: 6.18.39
            kernel-tag: v6.18.39
            force-9p: false
            version: v6.18-virtiofs
            compiler: default
            expect-pidfd: available

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Download Go modules for VM
        run: go mod download

      - name: Prepare kernel compiler
        id: kernel-compiler
        shell: bash
        run: |
          if [ "${{ matrix.compiler }}" = "gnu89" ]; then
            compiler="$RUNNER_TEMP/crosspty-gcc-gnu89"
            printf '%s\n' '#!/bin/sh' 'exec gcc "$@" -std=gnu89 -fcommon' > "$compiler"
            chmod +x "$compiler"
            hostcc="$(command -v gcc)"
          else
            compiler=""
            hostcc=""
          fi
          echo "cc=$compiler" >> "$GITHUB_OUTPUT"
          echo "hostcc=$hostcc" >> "$GITHUB_OUTPUT"

      - name: Run tests in Linux ${{ matrix.kernel }} VM
        uses: Kodecable/fork-vng-action@f03d101895186f2d35879fe3dda946aaa89da211
        with:
          name: crosspty-linux-${{ matrix.kernel }}
          kernel-url: https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git
          kernel-tag: ${{ matrix.kernel-tag }}
          version: ${{ matrix.version }}
          force-9p: ${{ matrix.force-9p }}
          disable-kvm: true
          qemu-opts: -cpu qemu64
          kconfig: ${{ matrix.kconfig }}
          cc: ${{ steps.kernel-compiler.outputs.cc }}
          hostcc: ${{ steps.kernel-compiler.outputs.hostcc }}
          vmlinux-h: false
          run: |
            set -eu
            uname -a
            case "$(uname -r)" in
              ${{ matrix.kernel }}*) ;;
              *) echo "unexpected guest kernel: $(uname -r)" >&2; exit 1 ;;
            esac
            CROSSPTY_EXPECT_PIDFD=${{ matrix.expect-pidfd }} go test -coverprofile coverage.txt -count=1 -v -timeout=10m ./...

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  go-test-windows:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-2022
          - windows-2025

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Run tests
        run: go test -coverprofile coverage.txt -count=1 -v -timeout=10m ./...

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  go-test-race:
    name: ubuntu-24.04 race
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Run race tests
        run: go test -coverprofile coverage.txt -race -count=1 -timeout=10m ./...

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  # GitHub-hosted Windows runners do not reliably grant the privileges
  # required by CreateProcessAsUser for a different local user token
  # (ERROR_PRIVILEGE_NOT_HELD / 1314). Re-enable this job on a self-hosted
  # Windows runner with the required privileges.
  #
  # windows-user-switch:
  #   name: windows-2025 user-switch
  #   runs-on: windows-2025
  #   timeout-minutes: 15
  #
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v5
  #
  #     - name: Setup Go
  #       uses: actions/setup-go@v6
  #       with:
  #         go-version-file: go.mod
  #
  #     - name: Create temporary local user
  #       shell: pwsh
  #       run: |
  #         $username = "cpty$($env:GITHUB_RUN_ID)$($env:GITHUB_RUN_ATTEMPT)"
  #         $passwordPlain = "Cpty!Aa1$([guid]::NewGuid().ToString('N'))"
  #         $password = ConvertTo-SecureString $passwordPlain -AsPlainText -Force
  #
  #         if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
  #           Remove-LocalUser -Name $username
  #         }
  #
  #         New-LocalUser -Name $username -Password $password -PasswordNeverExpires | Out-Null
  #
  #         "CROSSPTY_USER_SWITCH_USERNAME=$username" >> $env:GITHUB_ENV
  #         "CROSSPTY_USER_SWITCH_PASSWORD=$passwordPlain" >> $env:GITHUB_ENV
  #         "CROSSPTY_USER_SWITCH_DOMAIN=$env:COMPUTERNAME" >> $env:GITHUB_ENV
  #
  #     - name: Run user-switch integration test
  #       shell: pwsh
  #       run: |
  #         go test -run TestStartWithSysProcAttr_TokenUserSwitchIntegration -count=1 -v -timeout=10m .
  #
  #     - name: Cleanup temporary local user
  #       if: always()
  #       shell: pwsh
  #       run: |
  #         $username = $env:CROSSPTY_USER_SWITCH_USERNAME
  #         if ($username -and (Get-LocalUser -Name $username -ErrorAction SilentlyContinue)) {
  #           Remove-LocalUser -Name $username
  #         }
